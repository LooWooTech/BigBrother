@{
    Node node = ViewBag.Node;
    var isAdd = node.ID == 0;
    ViewBag.Title = isAdd ? "添加字段" : "修改字段";

}
<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title"><i class="glyphicon glyphicon-@(isAdd?"plus":"pencil")"></i>@(ViewBag.Title)</h3>
    </div>
    <div class="panel-body">
        <form id="form-node" action="/node/edit?id=@(node.ID)" method="post" class="form-horizontal">
            <div class="modal-body">
                <input type="hidden" name="FormId" value="@(node.FormID)" />
                <input type="hidden" name="ParentId" value="@(node.ParentID)" />
                <div class="form-group">
                    <label for="Type" class="control-label col-lg-3">字段名称</label>
                    <div class="col-lg-8">
                        <input type="text" name="Name" class="form-control" value="@(node.Name)" />
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <div class="col-lg-8 col-lg-offset-3">
                    <button type="submit" class="btn btn-primary"><i class="glyphicon glyphicon-ok"></i> 确定</button>
                </div>
            </div>
        </form>
    </div>
</div>
<script>
    $(function () {
        $("#form-node").registerSubmit(function () {
            $.closeWindow();
            $.refreshMain();
        });
    });
</script>
