@model Area
<option value="@(Model.ID)" @(Model.Checked ? "checked" : null)>
    @(new string('-', Model.Level))
    @(Model.Name)
</option>
@foreach (var child in Model.Children)
{
    Html.RenderPartial("_AreaOption", child);
}
